
# clone git repository
sudo apt-get install git # only if you do not have git already
git clone https://github.com/nicolas-fricke/semmul2013-group1.git
cd semmul2013-group1
git checkout for_python_2.6

# install python packages
sudo apt-get install python-pip # only if you do not have pip already
sudo pip install flask
sudo pip install flask_assets

sudo easy_install -U distribute
sudo pip install nltk
# ? wget http://argparse.googlecode.com/files/argparse-1.2.1.tar.gz # Download argparse
sudo pip install argparse
sudo pip install SimpleCV
sudo apt-get install python-scipy

# install pygame
sudo apt-get install libsdl1.2debian-all libsdl1.2-dev libsdl-console-dev
wget http://pygame.seul.org/ftp/pygame-1.9.1release.tar.gz
tar -xzvf pygame-1.9.1release.tar.gz
cd pygame-1.9.1release
sudo python setup.py

...

# start program
cd semmul2013-group1/src/
export PYTHONPATH=$PYTHONPATH:`pwd`

# unusedsudo
http://packages.debian.org/squeeze/all/python-flask/download